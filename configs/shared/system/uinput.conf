# modules-load drop-in — deploy to /etc/modules-load.d/uinput.conf
#
#   sudo cp configs/system/uinput.conf /etc/modules-load.d/uinput.conf
#
# Loads the `uinput` kernel module at boot so xremap's virtual keyboard device
# (/dev/uinput) is present. Pairs with 99-uinput.rules (group/perms).
uinput
